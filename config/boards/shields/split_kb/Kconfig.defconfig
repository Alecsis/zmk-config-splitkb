config ZMK_KEYBOARD_NAME
    default "SPLIT_KB" if SHIELD_SPLIT_KB_LEFT

config ZMK_SPLIT_ROLE_CENTRAL
    default y if SHIELD_SPLIT_KB_LEFT

config ZMK_SPLIT
    default y if SHIELD_SPLIT_KB_LEFT || SHIELD_SPLIT_KB_RIGHT
